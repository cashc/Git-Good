<%= form_for(@exercise_step) do |f| %>
  <% if @exercise_step.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exercise_step.errors.count, "error") %> prohibited this exercise_step from being saved:</h2>

      <ul>
      <% @exercise_step.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :exercise_id %><br>
    <%= f.number_field :exercise_id %>
  </div>
  <div class="field">
    <%= f.label :step_id %><br>
    <%= f.number_field :step_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
